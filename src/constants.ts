// Types imported as needed per module

// Constants for the application. No hardcoded mock data allowed.
export const APP_VERSION = "1.2.0";
export const DEFAULT_INVESTMENT = 10000000;
export const DEFAULT_DAYS = 360;
